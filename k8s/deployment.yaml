apiVersion: apps/v1
kind: Deployment
metadata:
  name: green-guard
  labels: { app: green-guard }
spec:
  replicas: 2
  selector:
    matchLabels: { app: green-guard }
  template:
    metadata:
      labels: { app: green-guard }
      annotations:
        kubectl.kubernetes.io/restartedAt: "55425a246fd9d7a7ca066bbd5e7cdaa7526f95d1"
    spec:
      containers:
        - name: app
          image: ghcr.io/just2courageous/green-guard:main   # <-- your GHCR image
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 5
            periodSeconds: 10
